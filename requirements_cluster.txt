# Minimal pinned dependencies for GLiNER cluster testing and ablation.
# Install torch separately for your CUDA/CPU target before this file:
#   python -m pip install torch==2.10.0 --index-url <pytorch index url>

# Core extraction
gliner==0.2.26
transformers==4.46.0
tokenizers==0.22.1
accelerate==1.11.0

# Data / artifact formats
pandas==2.3.2
pyarrow==23.0.0
numpy==1.26.4
tqdm==4.67.0

# Metrics / reporting
networkx==3.4.2
matplotlib==3.9.2

# ER / ablation
splink==4.0.12
duckdb==1.1.0
