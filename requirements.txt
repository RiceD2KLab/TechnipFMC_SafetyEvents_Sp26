# TechnipFMC Safety KG — Full development dependencies
# Install: pip install -r requirements.txt
# For HPC/cluster (lean GLiNER-only): pip install -r requirements_cluster.txt
# PyTorch: install separately for your CUDA/CPU target before this file

# ── Core pipeline ────────────────────────────────────────────────────────
gliner==0.2.26
pandas==2.3.2
numpy==1.26.4
networkx==3.4.2
tqdm==4.67.0
scikit-learn==1.6.1
scipy==1.13.1
pydantic==2.10.0

# ── Entity resolution (Splink + DuckDB) ─────────────────────────────────
splink==4.0.12
duckdb==1.1.0
rapidfuzz==3.10.0

# ── L2 enrichment + NLQ (LLM clients) ───────────────────────────────────
requests==2.32.0
anthropic==0.50.0
google-genai==1.0.0

# ── Event similarity ────────────────────────────────────────────────────
sentence-transformers==3.3.0
pykeen==1.10.2

# ── Event similarity (GNN, optional — requires torch_geometric) ─────────
# torch-geometric>=2.4.0       # install separately: depends on torch version

# ── Dashboard (Streamlit) ───────────────────────────────────────────────
streamlit==1.40.0
plotly==5.24.0
wordcloud==1.9.4
seaborn==0.13.2
matplotlib==3.9.2
pillow==11.0.0

# ── Dashboard (FastAPI backend) ─────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.32.0

# ── Data processing utilities ───────────────────────────────────────────
python-dateutil==2.9.0
PyYAML==6.0.2
pyvis==0.3.2

# ── Translation (optional) ──────────────────────────────────────────────
transformers==4.46.0
langid==1.1.6

# ── Neo4j graph loading (optional) ──────────────────────────────────────
neo4j==5.27.0

# ── Legacy: KG_spaCy (optional) ─────────────────────────────────────────
# spacy>=3.7.0
# textacy>=0.13.0
